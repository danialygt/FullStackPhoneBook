@model List<FullStackPhoneBook.Domain.Core.Tags.Tag>

@if (Model != null)
{

    <div class="row justify-content-end">

        @foreach (var item in Model)
        {
            <div class="col-xl-3 col-md-6 mb-4">
                <div class="bg-gradient-secondary card border-right-info shadow h-100 py-2 ">
                    <div class="card-body">
                        <div class="row no-gutters align-items-center">
                            <div class="col-auto">
                                <a asp-controller="Tag" asp-action="Delete" asp-route-id="@item.Id">
                                    <i class="fas fa-trash fa-2x text-danger"></i>
                                </a>
                            </div>
                            <div class="col mr-2 align-items-end">
                                <div class="text-lg font-weight-bold mb-1 text-gray-100 float-right">@item.Title</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }

    </div>
}
else
{
    <div asp-validation-summary="All" class="text-danger"></div>
}



